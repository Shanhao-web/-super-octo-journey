<% if user_signed_in? %>

<%else %>
<div class="jumbotron jumbotron-landing">
  <h1 class="display-4">Привет, студент!</h1>
  <p class="lead">Этот сайт тебе поможет (пока не придумал в чем).</p>
  <hr class="my-4">
  <p class="lead">
    <%= link_to "Регистрация",new_user_registration_path, class:"btn btn-primary" %>
    <%= link_to "Войти", new_user_session_path, class:"btn btn-secondary" %>
  </p>
</div>

<%end%>
